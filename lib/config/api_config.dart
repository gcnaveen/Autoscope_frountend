class ApiConfig {
  // Base host (no /api here)
  static const String host = 'https://6k651yup6b.execute-api.ap-south-1.amazonaws.com';

  // Your backend uses /api prefix in all endpoints
  static const String apiBaseUrl = '$host/api';
}
